<script src="~/js/moment/locale/ru.js" charset="UTF-8"></script>
<script>
var dataTable;

    $(document).ready(function () {
        if ("@Localizer["Locale"]" !== "Locale") {
            moment.locale('ru');
        } else {
            moment.locale('en');
        }

    loadDataTable();

});

function loadDataTable() {
    dataTable = $('#DT_TicketsLoad').DataTable({
        "ajax": {
            "url": "/Ticket/GetAll",
            "type": "GET",
            "datatype": "json"
        },
        "columns": [
            {
                "data": "id", "width": "20%", "render": function (data, type, row) {
                    return `<a href="/Ticket/Details/${data}" class="btn btn-primary">` + row.title + `</a>`
                }
            },
            { "data": "description", "width": "20%" },
            {

                "data": "created", "render": function (data) {
                    var date = moment(data).calendar();
                    return date;
                }
            },
            {
                "data": "id",
                "render": function (data) {
                    return `<div class="text-center">
                        <a href="/Ticket/AssignUser/${data}" class='btn btn-success text-white' style='cursor:pointer;'>Add or remove users</a>
                        <a class='btn btn-danger text-white' style='cursor:pointer;' onclick=Delete('/Ticket/Resolve?id='+${data})>@Localizer["Resolve"]</a></div>`;
                }, "width": "40%"
            }
        ],
        "language": {
            "emptyTable": "@Localizer["Nothing yet"]"
        },
        "width": "100%"
    });
}
</script>